[tool.poetry]
name = "genuary-2024"
version = "0.1.0"
description = ""
authors = ["JP Verkamp <jverkamp@dropbox.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
